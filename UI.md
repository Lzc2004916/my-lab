# 优化提示词

> 作为高级前端设计工程师，需在右侧工具栏中集成**可交互的 Markdown 标题样式调整功能**，支持 H1 至 H6 的**字体大小动态调节**及 H1 标题的**位置对齐（左 / 中 / 右）**，确保用户可通过可视化操作实时定制 Markdown 文档的排版样式。

---

## 一、工具栏功能设计

### 1. 标题级别样式调节

- 在右侧工具栏新增「Markdown 样式」面板，包含 H1 至 H6 的独立调节项。
- **字体大小调整**：
  - 为每个 Hx 级别提供**滑动条控件**或**数值输入框**，允许用户动态设置字体大小（支持 `px` / `rem` 单位）。
  - 需预设合理的最小 / 最大值范围（如 H1: `16px` ~ `48px`，H6: `12px` ~ `24px`），并显示当前值。
  - 调整时需实时同步渲染区对应标题的样式，支持**即时预览效果**。

### 2. H1 标题对齐方式

- 添加「对齐方式」下拉菜单或按钮组，提供「左对齐」「水平居中」「右对齐」三个选项。
- 切换对齐方式时，需立即更新渲染区 H1 标题的文本对齐样式

---

## 二、技术实现要点

### 1. 兼容性与健壮性

- 确保调整后的样式不干扰 Markdown 解析（如避免与代码块、列表等冲突）。
- 处理用户误操作（如输入非法字号值），提供默认值兜底。

---

## 三、工程化与扩展性

### 1. 模块化封装

- 将工具栏功能抽象为独立组件，便于复用与维护。

### 2. 配置化扩展

- 支持通过配置项新增其他样式属性（如颜色、行高），预留扩展接口。

### 3. 测试覆盖

- 编写单元测试与 E2E 测试，验证样式调整的实时性、数据同步及边界情况（如极端字号值、无选中内容时操作）。

---

## 五、交付标准

- 提供可交互的原型演示（如 Storybook），展示工具栏操作与实时渲染效果。
- 生成技术文档，说明组件 API、状态管理及样式类名规范。
- 覆盖 100% 核心功能的测试报告，包含不同 Markdown 场景的兼容性验证。

---

> **备注**：需平衡灵活性与编辑器性能，避免因频繁重渲染导致卡顿