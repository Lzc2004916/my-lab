「作为高级前端设计师，请为卡片主题系统设计一套标准化的视觉规范与实时交互机制。具体要求如下：

1. **核心视觉规范约束**：
    - 明确每种卡片主题必须包含且严格绑定以下核心视觉变量：固定的背景颜色、标题文本颜色（需包含完整的字体样式定义，如字重、字号、行高）、内容文本颜色（同样需包含完整的字体样式定义）。
    - 确保这些核心变量在主题切换时能够全局、无缝地同步生效。
2. **UI控件与数据双向绑定**：
    - 在界面配置面板中，必须实现并同步两个核心颜色选择器：背景渐变颜色选择器（支持自定义渐变角度、色标与透明度）和标题颜色选择器。
    - 建立数据双向绑定机制：当用户通过选择器修改颜色时，卡片需实时渲染预览；当切换预设卡片主题时，选择器的状态（当前选中的颜色值或渐变配置）需自动回显并同步更新。
3. **技术架构与工程化要求**：
    - 采用CSS自定义属性（CSS Variables）或Design Tokens管理上述核心视觉变量，确保主题配置的灵活性与组件样式的解耦。
    - 封装高内聚的颜色选择器组件，支持渐变配置的序列化与反序列化，方便后续的主题持久化存储（如保存至JSON配置或本地缓存）。
